# Committed fixture values (not real secrets): the suite asserts the plugin
# loads these through Vite's loadEnv fold with no vite.config boilerplate.
SESSION_SECRET=test-session-secret-0123456789abcdef
VITE_APP_NAME=EnvApp
