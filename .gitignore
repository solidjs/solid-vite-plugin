node_modules
dist

# Local env files (examples ship committed fixtures explicitly, e.g.
# examples/start-env/.env, which stays tracked because git ignores
# don't apply to already-tracked files)
.env
.env.*
!.env.example

# Stray local pnpm store (from installs with a store-path override)
.pnpm-store